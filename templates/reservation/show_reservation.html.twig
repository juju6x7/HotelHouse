{% extends 'base.html.twig' %}

{% block title %}Nos chambres - HouseHotel
{% endblock %}

{% block body %}

	<h1 class="text-center my-4">Nos chambres</h1>


	{% for chambre in chambres %}

		<div class="row">
			<div class="col-4 mx-auto my-4 text-center">

				<h3>
					{{ chambre.title }}
				</h3>

				<h4>
					{{ chambre.descriptionShort }}
				</h4>

				{{ chambre.descriptionLong }}
			</div>

			<div class="col-4 mx-auto my-4">
				<img class="round-corner" src="{{ asset('uploads/' ~ chambre.photo) }}" width="600px" height="400px" alt="">
			</div>
		</div>

		<div class="row">
			<div class="col-4 mx-auto my-4">
				{# <a href="{{ path('commande_chambre', {'id': chambre.id}) }}" class="btn btn-primary"><i class="fa-solid fa-plus"></i></i>Réserver une chambre</a> #}
				<a href="#" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Réserver une chambre</a>
			</div>
		</div>
	{% endfor %}
{% endblock %}
