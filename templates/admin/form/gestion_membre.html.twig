{% extends "base.html.twig" %}

{% block title %}Gestion des membre
{% endblock %}

{% block body %}

	
		<h1 class="text-center my-4">Gestion des membres</h1>
	

	<div class="row">
		<div class="col-8 mx-auto">

			{# {{ form_start(form) }}

                <div class="row">
                <div class="col-6">{{ form_row(form.title) }}</div>
                <div class="col-6">{{ form_row(form.subtitle) }}</div>
                <div class="col-6"></div>
                </div>

                {{ form_row(form.content) }}

				{{ form_row(form.category) }}

                {{ form_row(form.photo) }}
                

			{{ form_end(form) }} #}

		</div>
	</div>

{% endblock %}